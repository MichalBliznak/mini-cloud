spring:
  application:
    name: greeting
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://config:${CONFIG_PORT}
      fail-fast: true
      retry:
        max-attempts: 10
        initial-interval: 3000
        max-interval: 5000
